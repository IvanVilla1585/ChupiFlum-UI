@import "../../../stylus/imports.variables.styl"

.container-permissions
  .layout-permissions
    display-flex()
    flex-direction(column)

    .title-permissions
      sub-title()
      h2
        margin 0
        color $gray
