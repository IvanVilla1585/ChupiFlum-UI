@import "../../stylus/imports.variables.styl"

.header
  title()

  .title-header
    width percent(0.7)

    h1
      margin 0
      color $gray

  .user-data
    width percent(0.3)

    span
      color $gray
